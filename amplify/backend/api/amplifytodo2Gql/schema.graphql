type Todo @model @auth(rules: [{ allow: public }]) {
  id: ID!
  title: String!
  content: String!
  isDone: Boolean!
  writer: User!
  reader: [User]
}
type User @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  readTodo: [Todo]
}
